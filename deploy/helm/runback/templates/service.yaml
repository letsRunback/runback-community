apiVersion: v1
kind: Service
metadata:
  name: {{ include "runback.fullname" . }}
  labels:
    {{- include "runback.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "runback.selectorLabels" . | nindent 4 }}
